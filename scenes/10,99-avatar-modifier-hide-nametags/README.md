This example scene makes usage of `AvatarModifierArea` with the `AMT_HIDE_NAMETAGS` modifier only.

### SCENE TESTING

A single Avatar Modifier Area sits at the center of the scene, represented by a transparent cube. Walking into the area (with another avatar also inside it) should hide avatar nametags; walking out should show them again.
