The scene will lock the pointer every 10 seconds.

You can also lock the pointer by interacting with the first cube.

You can unlock the pointer by interacting with the second cube.

If you open the explore panel or any other full screen UI, and the scene requested to lock the pointer, the pointer should not lock until you come back to the scene.

If you open the developer console the pointer lock status should be printed correctly.