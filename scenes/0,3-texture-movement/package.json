{
  "name": "texture-movement-test-scene",
  "version": "1.0.0",
  "description": "Test scene for texture movement changing the texture's tiling and offset, plus a simple animation using tweens",
  "scripts": {
    "start": "sdk-commands start",
    "deploy": "sdk-commands deploy",
    "build": "sdk-commands build",
    "upgrade-sdk": "npm install --save-dev @dcl/sdk@latest",
    "upgrade-sdk:next": "npm install --save-dev @dcl/sdk@next"
  },
  "devDependencies": {
    "@dcl/asset-packs": "^1.20.0",
    "@dcl/js-runtime": "7.6.3",
    "@dcl/sdk": "^7.8.18"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=6.0.0"
  },
  "prettier": {
    "semi": false,
    "singleQuote": true,
    "printWidth": 120,
    "trailingComma": "none"
  }
}
